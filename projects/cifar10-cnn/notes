# Phase 2 · Day 5 学习笔记 —— CIFAR-10 图像分类实战（第一个像样的项目）

> 从"跟着搭"迈到"独立做项目"：把 MNIST 上的 LeNet 迁移到 CIFAR-10（10 类彩色真实物体），做成一个带 README 的独立 GitHub 项目。这是 v5.1「公开仓库」要求的第一个真正的项目，也是"迁移到新问题"（深度标准第 3 级）的第一次练习。

---

## 一、CIFAR-10 vs MNIST：难在哪

三个区别：**彩色（3通道）、真实物体、不居中**。所以同一个简单 CNN 在 MNIST 能到 98%，在 CIFAR-10 只有 60-70%——**这个掉分是正常且有意义的**，它说明真实图像识别本身就难，也是 README 里要如实写的。

---

## 二、迁移 LeNet 的两处关键改动

1. **第一层通道 1→3**：`nn.Conv2d(3, 6, 5)`，因为 CIFAR 是彩色（RGB）。不改会报通道错。
2. **fc1 输入维度重新追踪**：CIFAR 输入 32×32，要重新一路算形状（别背 MNIST 的 400）。

**核心教训：换数据/换结构，必须自己重新追踪形状，不能套用旧数字。** 不确定就在 forward 里 `print(x.shape)`。

**形状追踪练习记录**（padding 对尺寸的影响）：
- 3×3 核 + padding=1 → 保尺寸（32→32）。这是 3×3 卷积最常用配置。
- 5×5 核 + 无 padding → 缩 4（如 16→12）。
- 每个 MaxPool2d(2) → 减半。

---

## 三、数据标准化

`transforms.Normalize((0.5,0.5,0.5),(0.5,0.5,0.5))`：三个数对应 RGB 三通道，把像素从 0~1 变到 -1~1，让数据以 0 为中心，训练更稳。真实图像任务的标配预处理。

`transforms.Compose([...])`：把多个预处理步骤串成流水线，图片依次经过。

---

## 四、训练 = 复用五步循环

和 Day 4 一字不差。小改进：`momentum=0.9`（给 SGD 加惯性，收敛更好）、`running_loss`（打印整个 epoch 平均 loss，比只看最后一个 batch 更真实）。

---

## 五、README 让它成为"项目"

代码跑通只是一半，**写清 README 才叫项目**（v5.1 明确要求）。README 要写：做了什么、网络结构、真实结果、如何运行、下一步改进。项目单独建文件夹 `projects/cifar10-cnn/`，别和日常笔记混。

---

## 六、从结果找改进方向（研究思维练习）

模型只有 ~65%，按 v5.1 方法论"找到做得不好的地方 = 改进方向"。**要学会一次列多个 + 每个说清为什么**：

1. **加深网络（更多卷积层）**：浅网络只能提简单特征（边角），深一点才能组合出高级特征（轮廓、部件）。
2. **数据增强（随机翻转/裁剪/调色）**：让模型见到更多物体变体，提升泛化、减少过拟合。**性价比通常最高**。
3. **加 BatchNorm / 换 Adam 优化器**：训练更稳、收敛更好。
4. **训练更多轮 + 学习率调度**：10 epoch 可能没训够。

> 这道"列改进方向"就是 v5.1 第 6 节"找失败点=课题"的微缩预演。我的短板：以前只想到一个方向，要练成"系统地列多个并解释为什么"。

---

## 七、易错点

- 迁移时第一层还是 `in_channels=1` → 报通道错，CIFAR 是 3 通道。
- fc1 维度直接抄 MNIST 的 400 → 必须重新追踪形状。
- 看到 60-70% 以为跑失败 → CIFAR 本身难，正常，如实记录。
- 只交代码不写 README → 那不叫项目。
- 项目和日常笔记混在一起 → 单独建文件夹。
- 【网络坑】数据集国外源下不动：TUN 模式下清掉 WSL 的 http_proxy 变量让 TUN 接管，或用浏览器下载再拷进 WSL（`cp /mnt/c/...`）。

---

## 八、重点思考题（自测用，遮答案先讲）

**Q. CIFAR 迁移 LeNet 要改哪两处？**
> 第一层 in_channels 1→3（彩色）；fc1 输入维度重新追踪形状（别套 400）。

**Q. 3×3 核配 padding=1 对尺寸什么影响？**
> 尺寸不变（保尺寸），这是 3×3 卷积最常用配置。

**Q. 模型只有 65%，列三个改进方向及原因。**
> ①加深网络（提更高级特征）②数据增强（提泛化，性价比最高）③BatchNorm/Adam（训练更稳）。

**Q. 为什么同一个 CNN 在 CIFAR 比 MNIST 差很多？**
> CIFAR 彩色、真实物体、位置多变，比工整居中的手写数字复杂得多，简单网络表达力不够。

---

